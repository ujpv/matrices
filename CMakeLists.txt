CMAKE_MINIMUM_REQUIRED(VERSION 3.1)
PROJECT(matrices)

SET(CMAKE_CXX_STANDARD 14)

FIND_PACKAGE(Threads)
ADD_EXECUTABLE(mult main.cpp matrix.cpp)
TARGET_LINK_LIBRARIES(mult ${CMAKE_THREAD_LIBS_INIT} )
